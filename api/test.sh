# TODO implement test cases:
echo "No tests to run"

# For each endpoint:

# - Query a string value that contains a result in the data
# - Query a string value that contains a result in the data
# - Query a number value (probs duplicating for codewords as everything gets stringified)
# - Query an empty string
# - Query an empty string with no slash
# - Query a string value that contains multiple results in the data
